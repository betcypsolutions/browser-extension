# Üretilen/derlenen çıktılar
# manifest.json: build.sh manifest.{chrome,firefox}.json'dan üretir
manifest.json
# web-ext sign çıktısı (imzalı .xpi) ve AMO upload izi
web-ext-artifacts/
.amo-upload-uuid
# package.sh zip'leri
dist/
# dev.sh tarayıcıya özel symlink klasörleri (Load unpacked / Temporary Add-on)
build/
# Release'e yüklenecek xpi (GitHub Releases asset)
publish/

# Kişisel deploy notu (repoya/public'e gitmesin)
DEPLOY.txt

# Bağımlılık / sistem
node_modules/
.DS_Store
*.log

# NOT: updates.json BİLEREK izlenir (oto-güncelleme için raw URL'den servis edilir).
